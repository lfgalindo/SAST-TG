<?xml version='1.0' encoding='utf-8'?>

<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

	<session-factory>

		<!-- Database connection settings -->
		<property name="connection.driver_class">org.postgresql.Driver</property>
		<property name="connection.url">jdbc:postgresql://localhost:5432/sast</property>
		<property name="connection.username">postgres</property>
		<property name="connection.password">postgres</property>

		<!-- JDBC connection pool (use the built-in), quantidade de conexões simultaneas -->
		<property name="connection.pool_size">1</property>

		<!-- SQL dialect -->
		<property name="dialect">org.hibernate.dialect.PostgreSQL9Dialect</property>

		<!-- Enable Hibernate's automatic session context management -->
		<property name="current_session_context_class">thread</property>
		
		<!-- property name="hibernate.temp.use_jdbc_metadata_defaults">false</property -->

		<!-- Disable the second-level cache -->
		<property name="cache.provider_class">org.hibernate.cache.internal.NoCacheProvider</property>

		<!-- Echo all executed SQL to stdout (console) -->
		<property name="show_sql">true</property>

		<!-- Drop and re-create the database schema on startup -->
		<property name="hbm2ddl.auto">update</property>

		<!-- Mapeamento -->
		<!-- <mapping class="br.com.bsmotos.oficina.domain.Cliente" /> -->
		<mapping class="br.com.sast.domain.Caixa" />
		<mapping class="br.com.sast.domain.Cargo" />
		<mapping class="br.com.sast.domain.Fatura" />
		<mapping class="br.com.sast.domain.Manutencao" />
		<mapping class="br.com.sast.domain.Movimentacao" />
		<mapping class="br.com.sast.domain.Perfil" />
		<mapping class="br.com.sast.domain.Pessoa" />
		<mapping class="br.com.sast.domain.Plano" />
		<mapping class="br.com.sast.domain.PlanoPessoa" />

	</session-factory>

</hibernate-configuration>